# AuthFree - это DualAuth на любом хостинге без изменения команды запуска HytaleServer.jar

[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
![Hytale Version](https://img.shields.io/badge/Hytale-Early%20Access%202026-blue)

---

### ✖️ Проблема

Пропатченный HytaleServer.jar с
помощью [DualAuthPatcher](https://github.com/sanasol/hytale-auth-server/blob/master/patcher/README.md)
позволяет настроить DualAuth для игроков. При запуске
сервера в режиме DualAuth требуются аргументы `--session-token`
и `--identity-token` в команде запуска. Но хостинг-провайдеры часто не
предоставляют возможности добавить кастомные аргументы в команду запуска, что
делает невозможным активацию DualAuth при размещении сервера на хостинге.

### ✔️ Решение

**AuthFree** прямо на уровне сервера находит инжектированные классы DualAuth и передаёт им
`session-token` и `identity-token`, **не требуя изменения команды запуска сервера**.

### ⚙️ Установка

1. Скопируйте `HytaleServer.jar` (с поддержкой DualAuth
   от [sanasol](https://github.com/sanasol/hytale-auth-server)) на хостинг с заменой.
2. Скопируйте JAR-файл плагина в папку `mods/`.
3. Запустите сервер
4. Проверьте статус командой /auth_free status
5. При необходимости, измените конфигурацию в `mods/AuthFree/auth_free_settings.json`

При запуске сервера плагин:
- Получает свежие `sessionToken` и `identityToken` с вашего Auth-сервера (или публичного `sanasol.ws`).
- Передаёт их в API DualAuth.
- Кэширует токены и обновляет их с необходимой периодичностью.

Команда запуска сервера остаётся неизменной, а сервер работает в режиме DualAuth.

### 🖥️ Команды

Все команды доступны **только операторам сервера** (игрокам с правами `/op`).  
Обычные игроки получат отказ в доступе.

| Команда | Описание                                                                |
|---------|-------------------------------------------------------------------------|
| `/auth_free login`  | Запрашивает новые токены с Auth-сервера и применяет их.                 |
| `/auth_free status` | Отображает текущую конфигурацию, состояние токенов и время их действия. |
| `/auth_free reload` | Перезагрузить конфигурацию из `auth_free_settings.json`                 |

#### 📋 Пример вывода `/auth_free status`
```
=== AuthFree Status ===
Auth server: https://sessions.sanasol.ws/server/auto-auth
Server name: Hytale Server
Server ID: ваш-id-сервера
Auth on startup: true
Refresh interval: 12 hours
Session token: present (last refresh 342s ago)
Token expires in: 3258s
```

### ⚙️ Конфигурация

Файл `mods/AuthFree/auth_free_settings.json` создаётся **автоматически** при первом запуске.  
Вы можете отредактировать его в любом текстовом редакторе.

```json
{
  "auth_server": "https://sessions.sanasol.ws/server/auto-auth",
  "server_name": "AuthFree Server",
  "server_id": "ваш-сгенерированный-uuid",
  "auth_on_startup": true,
  "refresh_interval_hours": 12
}
```

| Поле | Описание                                                                                              |
|------|-------------------------------------------------------------------------------------------------------|
| `auth_server` | URL вашего Auth-сервера. По умолчанию - публичный `sanasol.ws`.                                       |
| `server_name` | Имя сервера, которое будет видно в админ-панели Auth-сервера.                                         |
| `server_id` | UUID сервера - генерируется автоматически при первом запуске (можно установить свой).                 |
| `auth_on_startup` | `true` - автоматически логиниться при старте сервера; `false` - только по команде `/auth_free login`. |
| `refresh_interval_hours` | Интервал автоматического обновления токенов (в часах). `0` – отключено.                               |

### 📜 Лицензия
[MIT License](https://github.com/DmFors/hytale-auth-free/blob/main/LICENSE).

### 🙏 Благодарности
- [hytale-auth-server](https://github.com/sanasol/hytale-auth-server)

### ⭐ Поддержка
Нашли баг или есть идеи? Создайте [Issue](https://github.com/dmfors/hytale-auth-free/issues).
